# Used by Vite for `npm run build` (copy to .env.production locally if needed).
# Vercel should set the same values as project environment variables.
VITE_API_BASE_URL=https://arrifection-happiness-exchange.hf.space
VITE_APP_NAME=Happiness Exchange Admin
