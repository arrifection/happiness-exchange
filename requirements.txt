# fastapi - web framework
fastapi==0.115.12

# uvicorn - ASGI server to run FastAPI
uvicorn[standard]==0.34.2

# motor - async MongoDB driver (built on top of pymongo)
motor==3.7.0

# dnspython - required for mongodb+srv:// Atlas URI resolution
dnspython==2.7.0

# pydantic-settings - env-based config management
pydantic-settings==2.9.1

# python-dotenv - loads .env file automatically
python-dotenv==1.1.0

# python-jose - creates and verifies JWT access tokens
python-jose[cryptography]==3.5.0

# bcrypt - hashes and verifies passwords securely
bcrypt==5.0.0
# email-validator - enables EmailStr validation in Pydantic
email-validator==2.2.0

# httpx - required by FastAPI/Starlette TestClient for backend tests
httpx==0.28.1

# slowapi - IP/user rate limiting for auth and write endpoints
slowapi==0.1.9

# Pillow - image validation and re-encoding for uploads
Pillow==11.2.1

# python-multipart - parses file uploads for FastAPI UploadFile endpoints
python-multipart==0.0.20

# pytest - integration tests and marker support
pytest==8.3.5
pytest-asyncio==0.25.3
