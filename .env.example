# Frontend production API — REQUIRED for Vercel deploys.
# The live backend runs on Hugging Face Spaces (not Vercel /api).
#
# VITE_ audit (safe to expose in browser bundle):
#   VITE_API_BASE_URL — public HF Space URL only (no secrets)
# Never put API keys, JWT secrets, admin tokens, or passwords in VITE_ variables.
VITE_API_BASE_URL=https://arrifection-happiness-exchange.hf.space
